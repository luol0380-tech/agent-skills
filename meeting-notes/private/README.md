# Private local context

Store meeting-specific style preferences, confidential terminology mappings, and redaction rules here. Actual private files and `assets/` are ignored by Git.

Copy the `*.example.md` file, remove `.example` from the filename, and fill it locally; or run the repository bootstrap script.
