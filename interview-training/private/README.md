# Private local context

在本地保存候选人经历、面试历史、个人表达偏好和真实案例。实际文件与 `assets/` 不会被 Git 提交。

可以复制同目录的 `*.example.md`，去掉文件名中的 `.example` 后填写；也可以运行仓库根目录的 `scripts/bootstrap_local.py`。公开 Skill 在没有这些文件时仍可运行，但不会拥有个性化背景。
