# Private local context

Store real stories, transcripts, participant relationships, photos, location references, approved character cards, branded props, and authorization notes here. Actual private files and `assets/` are ignored by Git.

Copy the `*.example.md` files, remove `.example` from their filenames, and fill them locally; or run the repository bootstrap script.
