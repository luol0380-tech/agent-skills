# Private local context

Store brand position, personal voice, real writing samples, account details, screenshots, portraits, and other non-public assets here. Actual private files and `assets/` are ignored by Git.

Copy the `*.example.md` files, remove `.example` from their filenames, and fill them locally; or run the repository bootstrap script.
